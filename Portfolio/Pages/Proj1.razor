@page "/proj1"
@using Portfolio.Services
@using Portfolio.Shared.Components
@using Portfolio.Shared.Layouts
@using Portfolio.Model
@using Portfolio.Client
@layout ProjectLayout
@inject AppState AppState;
@inject NavigationManager NavigationManager

<PageIsland IslandTitle="Project 1" Id="proj1">
    <ChildContent>
        Dit is de testcontent van Project 1
    </ChildContent>
</PageIsland>

@code {

    private static (string, string)[]? Links => new[]
    {
        ("#proj1", "Project 1"),
    };

    private static readonly HeaderData HeaderData = new()
    {
        ImagePath = "icon-192.png",
        Title = "Project 1",
    };


    protected override void OnInitialized()
    {
        AppState.Links = Links;
        AppState.PageIcon = StaticData.DefaultPageIcon;
        AppState.HeaderData = HeaderData;
    }

}